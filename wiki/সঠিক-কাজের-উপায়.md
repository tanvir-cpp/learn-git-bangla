# কাজের সঠিক নিয়ম (Daily Workflow) 🛠️

প্রফেশনালরা যেভাবে গিট ব্যবহার করে কাজ করে, তার একটা সহজ গাইড। এই নিয়ম ফলো করলে কোড হারানো বা জগাখিচুড়ি পাকানোর ভয় থাকবে না।

## ১. সকালে কাজ শুরু করার আগে
সবার আগে `git pull` করে নিন। এতে গিটহাবের লেটেস্ট কাজ আপনার কাছে চলে আসবে।
```bash
git pull origin main
```

## ২. নতুন কোনো কাজ শুরু করলে
সরাসরি মেইন ব্রাঞ্চে হাত না দিয়ে একটা আলাদা ব্রাঞ্চ খুলে নিন। এতে মেইন কোড সেফ থাকে।
```bash
git switch -c feature/amar-notun-kaj
```

## ৩. একটু পর পর সেভ (কমিট)
সারাদিন কাজ শেষে একটা বড় কমিট না করে, ছোট ছোট কাজের পর পর কমিট করুন।
```bash
git add .
git commit -m "docs: dictionary te notun shobdo add kora holo"
```

## ৪. কাজ জমা দেওয়া বা পুশ করা
কাজ পুরোপুরি হয়ে গেলে ব্রাঞ্চটা গিটহাবে পাঠিয়ে দিন এবং একটা **Pull Request** ওপেন করুন।
```bash
git push origin feature/amar-notun-kaj
```

## ৫. সব শেষে মুছে ফেলা
মার্জ হয়ে যাওয়ার পর ব্রাঞ্চটা ডিলিট করে দিয়ে পিসি পরিষ্কার রাখুন।
```bash
git switch main
git pull
git branch -d feature/amar-notun-kaj
```

এই সহজ নিয়মগুলো অভ্যেস করলে আপনিও হযে উঠবেন একজন প্রো-ডেভেলপার! 😎
