# কমান্ডের শর্টকাট (কুইক রেফারেন্স) ⚡

বড় বড় কমান্ড মুখস্থ করার দিন শেষ! হাতের কাছে এই লিস্টটা রাখুন।

## 🚀 শুরু করতে হলে
- `git init`: আপনার বর্তমান ফোল্ডারটাকে গিটের আন্ডারে নিয়ে আসুন।
- `git clone [url]`: গিটহাবে থাকা কোনো প্রোজেক্ট নিজের পিসিতে নামিয়ে ফেলুন।

## 🛠️ কাজ করার সময়
- `git status`: এখন কি অবস্থা? কি কি চেইঞ্জ হলো? দেখে নিন।
- `git add .`: যা যা বদলালেন সবগুলোকে গিটের বাস্কেটে ভরুন।
- `git commit -m "মেসেজ"`: একটা নাম দিয়ে কাজটা পাকাপোক্তভাবে সেভ করুন।
- `git log --oneline`: কাজের ইতিহাস সংক্ষেপে এক লাইনে দেখে নিন।

## 🌿 ব্রাঞ্চ নিয়ে কাজ
- `git switch -c [name]`: নতুন ব্রাঞ্চ খুলে সরাসরি সেখানে চলে যান।
- `git switch [name]`: পুরানো কোনো ব্রাঞ্চে ফিরে যান।
- `git merge [name]`: অন্য ব্রাঞ্চের কাজগুলো মেইন ব্রাঞ্চে জোড়া লাগিয়ে দিন।

## ☁️ গিটহাবের সাথে লেনদেন
- `git push origin main`: নিজের সব কাজ গিটহাবে জমা দিন।
- `git pull`: গিটহাব থেকে নতুন কাজগুলো নিজের পিসিতে নিয়ে আসুন।

## 🆘 বিপদে পড়লে
- `git stash`: অর্ধেক করা কাজ পকেটে রেখে পরিষ্কার হয়ে যান।
- `git stash pop`: পকেট থেকে কাজ বের করে আবার শুরু করুন।
- `git restore [file]`: ভুল করে কিছু ডিলিট করলে সেটা ফেরত আনুন।

> **টিপ:** এই পেজটা ব্রাউজারে পিন করে রাখুন, দরকার হলেই এক পলক দেখে নিবেন!
