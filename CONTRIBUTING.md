# কন্ট্রিবিউশন গাইড

এই প্রোজেক্টে কন্ট্রিবিউট করতে চাও? অসাধারণ! এই গাইডটি পড়ে নাও, তাহলে পুরো প্রসেসটা সহজ হয়ে যাবে।

## 🤝 কি ধরনের কন্ট্রিবিউশন নেওয়া হয়?

সব ধরনের কন্ট্রিবিউশন স্বাগত! যেমন:

- 📝 **বানান ও ব্যাকরণ ঠিক করা** — বাংলা বানান ভুল, টাইপো
- 📖 **কন্টেন্ট উন্নতি** — ব্যাখ্যা আরো সহজ করা, উদাহরণ যোগ করা
- 🆕 **নতুন টপিক** — নতুন বিষয় যোগ করার প্রস্তাব
- 🐛 **ভুল তথ্য সংশোধন** — কোনো কমান্ড বা তথ্যে ভুল থাকলে
- 🎨 **ডিজাইন উন্নতি** — বইয়ের লেআউট বা স্টাইলিং উন্নত করা
- 🌐 **অনুবাদ** — অন্য ভাষায় অনুবাদ

## 🚀 কিভাবে কন্ট্রিবিউট করবে?

### ছোট চেইঞ্জ (টাইপো, বানান ভুল)

1. গিটহাবে ফাইলটি খোলো
2. **Edit** (পেন্সিল আইকন) ক্লিক করো
3. চেইঞ্জ করো
4. **Propose changes** ক্লিক করো
5. **Create pull request** ক্লিক করো

### বড় চেইঞ্জ (নতুন টপিক, বড় আপডেট)

#### ধাপ ১: ফর্ক ও ক্লোন

```bash
# রিপো ফর্ক করো (গিটহাবে Fork বাটন)

# ফর্ক করা রিপো ক্লোন করো
git clone https://github.com/YOUR-USERNAME/learn-git-bangla.git
cd learn-git-bangla
```

#### ধাপ ২: নতুন ব্রাঞ্চ তৈরি করো

```bash
git switch -c fix/your-change-description
```

ব্রাঞ্চ নামের কনভেনশন:

- `fix/typo-in-readme` — ছোট ফিক্স
- `content/add-git-bisect` — নতুন কন্টেন্ট
- `improve/better-examples` — বিদ্যমান কন্টেন্ট উন্নতি

#### ধাপ ৩: চেইঞ্জ করো

```bash
# mdBook লোকালি চালাও (অপশনাল, কিন্তু রেকমেন্ডেড)
mdbook serve --open

# ফাইল এডিট করো
# ...
```

#### ধাপ ৪: কমিট ও পুশ করো

```bash
git add .
git commit -m "fix: README তে বানান ভুল ঠিক করেছি"
git push origin fix/your-change-description
```

#### ধাপ ৫: Pull Request তৈরি করো

গিটহাবে গিয়ে **Compare & pull request** ক্লিক করো।

## 📝 কন্টেন্ট লেখার নিয়ম

এই বইটি বাংলায় লেখা। কন্টেন্ট লেখার সময় এই নিয়মগুলো মেনে চলো:

### ভাষা

- **কথ্য বাংলা** ব্যবহার করো — ফর্মাল না, আড্ডার মতো
- পাঠককে **"তুমি"** বলে সম্বোধন করো
- ইংরেজি থেকে সরাসরি অনুবাদ **করো না** — নিজের ভাষায় লেখো
- টেকনিক্যাল শব্দ ইংরেজিতে রাখো, বন্ধনীতে বাংলা দেওয়া যেতে পারে

### ফরম্যাটিং

- প্রতিটা ফাইলে একটাই `# H1` হেডিং থাকবে
- হেডিংয়ের আগে ও পরে ফাঁকা লাইন রাখো
- কোড ব্লকে সবসময় ল্যাঙ্গুয়েজ উল্লেখ করো (````bash`, ````diff`)
- লিস্টের আগে ও পরে ফাঁকা লাইন রাখো
- মার্কডাউন লিন্টিং রুল মেনে চলো

### স্ট্রাকচার

প্রতিটা টপিকে এই প্যাটার্ন ফলো করো:

1. **কি জিনিসটা** — সহজে বুঝিয়ে বলো
2. **কেন দরকার** — রিয়েল লাইফ উদাহরণ দাও
3. **কিভাবে করবে** — কমান্ড সহ দেখাও
4. **উদাহরণ** — প্র্যাকটিক্যাল উদাহরণ দাও

## 🔍 কমিট মেসেজ কনভেনশন

```
type: বাংলায় বা ইংরেজিতে ছোট বর্ণনা
```

টাইপ:

- `docs:` — কন্টেন্ট যোগ বা আপডেট
- `fix:` — ভুল ঠিক করা
- `style:` — ফরম্যাটিং চেইঞ্জ
- `chore:` — টুলিং, কনফিগ চেইঞ্জ

উদাহরণ:

```
docs: add git-bisect chapter
fix: correct typo in git-merge page
style: fix heading spacing in SUMMARY.md
chore: update mdbook version in workflow
```

## 📋 Pull Request চেকলিস্ট

PR পাঠানোর আগে চেক করো:

- [ ] ব্রাঞ্চ `main` থেকে তৈরি করেছো
- [ ] মার্কডাউন ফরম্যাটিং ঠিক আছে
- [ ] বানান চেক করেছো
- [ ] নতুন ফাইল হলে `SUMMARY.md` তে যোগ করেছো
- [ ] কমিট মেসেজ কনভেনশন ফলো করেছো

## ❓ প্রশ্ন আছে?

কোনো প্রশ্ন থাকলে [GitHub Issues](https://github.com/tanvir-cpp/learn-git-bangla/issues) এ জানাও। আমরা সাহায্য করবো!

## 🙏 ধন্যবাদ

তোমার কন্ট্রিবিউশন এই প্রোজেক্টকে আরো ভালো করবে। সবাই মিলে বাংলায় একটা দারুণ গিট গাইড তৈরি করি! ❤️
